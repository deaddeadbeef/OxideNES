# M4 IP Compliance Review - 2026-05-23

Scope: all tracked repository files, release workflow inputs, installer source declarations, documentation, examples, generated-test policy, and ignored local build artifacts visible in the workspace.

## Result

Status: pass after M4 cleanup.

## Changes Made During Review

- Removed the tracked `nestest.nes` binary fixture to avoid relying on externally sourced ROM redistribution status in a public repository.
- Replaced the binary CPU fixture test with `tests/cpu_program_tests.rs`, which builds deterministic in-memory iNES data from `tests/common/synthetic_rom.rs`.
- Removed the `.gitignore` exception that allowed `nestest.nes` to be tracked.
- Added `scripts/ip_compliance_audit.py` and wired it into CI and release workflows.
- Removed the unnecessary third-party trademark keyword from Cargo package metadata.
- Updated stale public-facing text in README, fixture policy, IP policy, and the Lua example command.

## Repository File Review

- ROMs/BIOS/save files: no tracked ROM, BIOS, SRAM, or save files remain.
- Screenshots/recordings/art/music: no tracked screenshots, recordings, game artwork, manuals, music, logos, icons, or generated third-party art are present.
- Docs and examples: user-facing docs repeatedly state that ROMs are not included, users must provide legally obtained files, and issues must not include ROM uploads or proprietary media.
- Built-in metadata: `src/romdb.rs` still contains factual CRC-keyed compatibility metadata only. It includes title strings for identification, not artwork, descriptions, publisher claims, screenshots, manuals, or ROM content.
- Installer inputs: active WiX file inputs are limited to `wix/License.rtf` and the built `oxidenes.exe`; the release asset checker enforces this.
- Ignored workspace artifacts: local `target/`, `perf_output.txt`, and `test_precedence.pdb` are ignored and not part of the public repository.

## Ongoing Rules

- Do not add binary ROM fixtures. Use generated fixtures unless maintainers explicitly approve a redistributable non-ROM binary asset.
- Keep release screenshots and recordings limited to homebrew, public-domain, or original test content.
- Keep third-party game and platform names factual and nominative, with no endorsement language.
- Run `python scripts/ip_compliance_audit.py` before 1.0 RC tags and before any GitHub Release.
